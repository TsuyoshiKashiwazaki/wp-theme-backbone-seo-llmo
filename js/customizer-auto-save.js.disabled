/**
 * カスタマイザー手動保存機能
 * 公開時にカラー設定をテーマとして手動で保存（ユーザーの明示的な操作時のみ）
 */

console.log('Loading manual save theme on publish script...');

(function($) {
    'use strict';
    
    // 手動保存機能は削除 - 自動実行は一切行わない
    console.log('Manual save only - no automatic saving');
    
})(jQuery);